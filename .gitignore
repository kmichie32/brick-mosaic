# Generated by `npm run preview`. Deliberately NOT a blanket *.png -- that
# would silently swallow a favicon, an og:image, or any real asset.
/preview.png

# There are no dependencies, but keep this honest if that ever changes
node_modules/

# Per-machine Claude Code permissions, not project config
.claude/settings.local.json

.DS_Store
